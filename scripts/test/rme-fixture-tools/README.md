# scripts/test/rme-fixture-tools

Last updated (UTC): 2026-02-14

Support tools used by test harnesses.

## Contents
- `fake_fallout_runner`: deterministic fallback runner for integration tests.

## Notes
- `fake_fallout_runner` writes logs under `RME_LOG_FILE` or `RME_WORKING_DIR` (no fixed system temp path required).
